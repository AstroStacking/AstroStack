FILE(GLOB ASTRO_IO_SRC
    IO/io.cpp
)

FILE(GLOB ASTRO_IO_HEADERS
    IO/config.h
    IO/io.h
)

SET(ASTRO_IO_LIBS ${ITK_LIBRARIES})
if(EXIV2_FOUND)
    LIST(APPEND ASTRO_IO_LIBS exiv2lib)
endif()

ASTRO_add_library(
    AstroIO
    NAME AstroIO
    FOLDER IO
    SRC ${ASTRO_IO_SRC}
    HEADERS ${ASTRO_IO_HEADERS}
    LIBRARIES ${ASTRO_IO_LIBS}
)
